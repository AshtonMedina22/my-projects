# Lesson Notes

## Checkpoint Summary

1. Add a page title with `st.title()` and markdown with `st.markdown()`.
2. Add a sidebar with `st.sidebar.title()` and `st.sidebar.markdown()`.
3. Add a text area with key `"user_question"` and write the user question.
4. Add two columns, a number input, and a model selectbox with `"gpt-3"` and `"gpt-4"`.
5. Add a button named `"Get Answers"` and display the placeholder RAG output.

## Run Command

```bash
streamlit run main.py
```

